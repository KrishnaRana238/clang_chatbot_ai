services:
  - type: web
    name: clang-ai-chatbot
    env: python
    plan: free
    buildCommand: ./build.sh
    startCommand: gunicorn chatbot_project.wsgi:application --host 0.0.0.0 --port $PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DJANGO_SETTINGS_MODULE
        value: chatbot_project.settings
      - key: DEBUG
        value: false
      - key: SECRET_KEY
        generateValue: true
